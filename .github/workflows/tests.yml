name: tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        # (Node runs the Notes and Reminders scripts against a fake object model)
        with:
          node-version: "20"
      - name: Install
        run: pip install -e ".[test]"
      - name: Offline tests
        run: pytest tests --ignore=tests/integration -q

  # Integration tests and the benchmark against a local Dovecot + SMTP sink + Radicale. The integration step reports but does not
  # fail the job; the benchmark writes its tables to the job summary and uploads them (gh run download <id> -n bench).
  bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q dovecot-imapd
          pip install -e ".[test]"
      - name: Start the local stack
        run: sudo env "PATH=$PATH" bash dev/start_local_stack.sh
      - name: Integration tests
        continue-on-error: true
        run: pytest tests/integration -q
      - name: Benchmark
        run: |
          python dev/bench.py --local --runs 5 --idle-calls 5 --gap 20 --out bench.md --json bench.json
          python dev/bench.py --local --no-seed --runs 5 --idle-calls 0 --latency-ms 40 --out bench-40ms.md --json bench-40ms.json
          python dev/tool_surface.py > surface.md
          cat bench.md bench-40ms.md surface.md >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        if: always()
        with:
          name: bench
          path: |
            bench*.md
            bench*.json
            surface.md
